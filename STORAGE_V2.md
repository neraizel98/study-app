# 저장 구조 v2 — 2026-09-17

## 데이터 보존
- 기존 퀴즈/오답을 IndexedDB의 개별 기록으로 원자적으로 이전한다. 원래 localStorage 문자열은 IndexedDB의 archive 항목에 그대로 보존하고, 트랜잭션 성공 후에만 용량이 큰 localStorage 사본을 정리한다.
- 기존 서버의 `data/reports`, `data/wrongAnswers` 문서는 삭제하거나 덮어쓰지 않는다. 새 문서의 저장이 모두 성공해야 `data/storageV2` 완료 표시를 만든다.
- 이전 중 종료되면 저장 확인 지문을 이용해 이어서 진행한다. 이전 완료 후 구버전 탭이 남긴 로컬 기록도 다음 실행에서 병합한다.
- 최초 답안과 오답 이력은 삭제하지 않는다. 오답 목록에서 제거한 항목은 삭제 표시로 숨기고 원래 이력을 보존한다.
- 백업 가져오기는 현재 기록과 병합한다. 백업에 없는 9월 16일의 상세 기록을 추정 생성하지 않는다.

## 저장과 조회
- 기기: IndexedDB 개별 레코드 + 서버 저장 확인 지문. 변경된 행만 쓴다. 미확인 기록은 앱 재시작 후에도 남는다.
- 서버: `quizRecords` 회차별 요약, `wrongRecords` 항목별 요약, `recordBodies` 무손실 압축 상세, `dailyRecords` 날짜별 기기 누적값.
- 원문은 SHA-256으로 검증하며, 큰 상세 기록도 80,000자 이하 조각으로 나누어 단일 문서 한도를 넘지 않게 한다. 과거 복구 사본과 불변 상세 버전은 보수적으로 보존한다.
- 변경 없는 기록은 서버 읽기/쓰기를 생략한다. 변경 업로드는 기존 서버 기록과 병합하고, 동시 수정이 있으면 성공 처리하지 않고 재시도한다.
- 동기화 수신은 서버 시각 체크포인트 이후 변경분을 페이지 단위로 받는다. 새 기기의 첫 복원은 기존 이력을 받아 오프라인 사용이 가능하도록 한다.
- 관리자 첫 조회는 최근 퀴즈 20회와 복습 도래 항목 최대 50개이다. 이전 퀴즈는 20회씩, 상세 답안은 클릭할 때만 읽는다. 주간 화면은 선택 주와 8주 추세에 필요한 기간의 요약만 읽는다.
- 날짜별 활동은 기기별 증가량을 단조 증가 값으로 병합하여 재전송 중복을 막는다. 날짜 전환 전의 기존 일일 통계도 별도 보존한다. 과거에 아예 저장되지 않은 날짜는 미학습으로 단정하지 않는다.

## 운영 전환
- 배포 후 학습 기기에서 새로고침하고 클라우드 연결 상태로 열어야 그 기기에만 남은 기록이 서버로 이전된다. 초기 이전은 일반 동기화보다 오래 걸릴 수 있다.
- 서버 이전이 끝나기 전 관리자 화면은 기존 서버 기록을 표시한다. 종료 후 새 조회 방식으로 전환한다.
- 기기 저장 실패와 서버 재시도를 구분하여 표시한다. 실패한 업로드는 완료로 표시하지 않는다.
- 새 문항을 콘텐츠 ID만으로 재구성하는 방식은 과거 표현의 정확한 보존을 위해 강제하지 않았다. 현재는 원문 무손실 압축과 상세 분리로 중복 전송량을 줄인다.

## 검증
- 자동 검사 20개 파일: 기존 회귀 검사와 IndexedDB 이전/재시작, 원본 사본, 구버전 탭 병합, 변경분 저장, 페이지 조회, 실패 재전송, 날짜별 중복 방지.
- 제공된 9월 17일 백업의 모든 퀴즈/최초 답안/오답 이력을 이전 전후 그대로 비교했다. 우준 퀴즈 1,011회 보존.
- 실제 Edge 브라우저의 격리된 시험 환경에서 홈·영어·문법·독해·한자·오답 페이지를 순회하며 1,011회 유지와 페이지 오류 0건을 확인했다.
- 실제 로그인 계정의 서버 이전 완료 여부는 학습 기기가 새 버전을 실행한 뒤 확인해야 한다. 공개 배포 완료와 사용자 데이터 이전 완료는 서로 다른 단계다.
