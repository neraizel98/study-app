# 보호자 학습 계획 설정

> 2026-09-23 이후 과목별 자동 추천·보호자 지정 범위, 출판사 제거, 시작된 오늘 계획 보존 기준은 `PARENT_SCOPE_SETTINGS.md`가 우선합니다. 아래 내용은 최초 하루 목표 설정의 기록입니다.

- 저장 경로: `learnerPlanSettings/{learnerId}` (기존 `users` 재귀 권한과 분리)
- 보호자 설정: 하루 목표 30/45/60분, 학년 1~12, 학기 1/2, 선택 출판사
- 기본값: 하루 45분, 우준 초등 6학년 2학기, 출판사 미확인
- 학습자 화면: 로컬 캐시를 즉시 표시하고 원격 설정은 비동기로 갱신합니다. 원격 읽기가 실패하면 마지막 캐시 또는 기본값을 유지합니다.
- 기존 오늘 계획: 설정이 바뀌면 총 시간, 과목별 시간, 학년 정보만 갱신합니다. 단원, 상태, 세션, 시작·완료·제출 시각은 유지합니다.
- 권한: 읽기는 `canUse(learnerId)`, 쓰기는 실제 Google UID의 `access.role == admin`일 때만 허용합니다. 로컬 표시 이름만 `우준아빠`로 바꿔서는 저장할 수 없습니다. 반대로 같은 관리자 Google UID를 공유하면 로컬에서 아이 이름을 선택해도 서버는 관리자 요청으로 판단하므로 관리자 Google 계정을 공유하면 안 됩니다.
- 별도 설정: `studyTimeConfig`는 퀴즈 잠금용 최소 학습 시간으로 계속 분리해 관리합니다.

검증은 `parent-plan-settings.test.js`, `learning-upgrade.test.js`, 전체 회귀 테스트로 수행합니다. Firestore 규칙은 배포 전에 Firebase CLI로 별도 컴파일해야 합니다. 로컬 단위 테스트는 실제 Firebase 계정 또는 운영 데이터를 변경하지 않습니다.
