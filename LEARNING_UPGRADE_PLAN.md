# 오늘의 학습·주간 실력 확인 구현 기준

사용자 승인: 2026-09-18. 우준은 초등학교 6학년 2학기이며 교과서 출판사는 미확인이다. 중학교 대비를 위해 초등 기본기를 확인하고, 확인된 학습 범위를 바탕으로 다음 내용을 추천한다.

## 승인 범위

- 국어(reading)·영어(english)·수학(math) 하루 30/45/60분 계획. 배분은 각각 8/10/12, 12/15/18, 15/20/25분. 기본 45분.
- 복습 도래 → 근거 있는 취약 개념 → 현재 학습 단원 순으로 추천하고 이유를 표시한다. 자료가 없으면 단원을 선택하게 한다.
- 시간은 예상 학습량이다. 시간 채우기용 잠금이나 자동 정답·학습완료 처리를 하지 않는다. 방치 시간은 제외한다.
- 같은 기기·계정에서 중단한 문항과 답안을 복원한다. 다른 기기의 초안 복원은 첫 버전의 보장 범위가 아니다.
- 수학 공식 퀴즈를 최초 답안·재시도·오답 기록에 연결하며 기존 시간 기록을 중복 집계하지 않는다.
- 주간 평가는 실제 학습 근거가 있는 단원에서 고정 난이도로 출제한다. 가능한 검증된 동등 문항을 사용하고, 신규 문항이 부족하거나 과거 문항을 재사용하면 제한을 명시한다.
- 평가 진행 중에는 힌트와 정답을 제공하지 않고 첫 제출 뒤 해설한다. 평가 결과는 다음 학습 추천에 쓰되 일반 적응형 승급에서 제외한다.
- 부모 화면은 시간, 최초 풀이, 평가 범위·문항 수, 다음 추천, 기록 상태를 구분한다. 근거 없는 등수나 실력 수치를 만들지 않는다.

## 데이터 계약 주의

- 기존 saveQuizResult의 isCompleted는 전부 정답 여부이다. 제출 여부는 metadata.submittedAt으로 따로 관리한다.
- 작성 중 답안을 보고서에 미리 저장하면 최초 답안이 고정되므로 중간 상태는 별도 초안에 보관한다.
- 질문·답·선택답·문맥·콘텐츠 버전·힌트 여부를 보존한다. 재시도는 최초 기록을 바꾸지 않는다.
- 기존 기록을 초기화하거나 과거 공식 풀이를 추정 생성하지 않는다. 온라인 재시도와 중복 제출은 같은 결과를 중복 기록하지 않아야 한다.
- 교과서 미설정과 현재 진도 미확인을 명시한다. 실제 학교 시험 범위가 입력되기 전에는 시험 범위와 일치한다고 주장하지 않는다.

## 완료 검증

- 시간 배분과 추천 우선순위, 자료 부족 표시.
- 계정 격리, 새로고침·오프라인 초안 복원, 고정 문항 유지.
- 중복 제출 방지, 최초 답안 보존, 평가의 adaptive 제외.
- 미학습 단원 제외, 문항 부족·재사용 표시, 평가 중 정답 비공개.
- 공식과 일반 수학 기록의 충돌 및 시간 중복 집계 방지.
- 기존 자동 검사와 제공 백업의 기록 보존 검사, 모바일 크기 브라우저 흐름 확인.

## 역할

설계 검토는 GPT-6 Astra, 구현과 테스트는 GPT-5.6 Sol이 담당한다. 구현 범위·제약·검증 결과는 실제 완료한 내용을 기준으로 별도 기록한다.
