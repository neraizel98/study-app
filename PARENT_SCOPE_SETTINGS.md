# 보호자 학습 범위 설정

관리자는 학습자별 하루 목표(30·45·60분), 학년·학기와 국어 독해·영어 단어·수학 범위를 설정할 수 있다. 영어 문법과 수학 공식은 이 범위 설정에 포함되지 않는다. 국어 자료는 현재 중1 독해 Lv. 1만 제공하며, 수학은 실제로 탑재된 초6·중1 학기 자료만 표시한다. 지원 자료가 없는 학년은 임의의 단원을 만들지 않고 `지원 단원 없음`으로 표시한다.

각 과목 범위는 다음 두 방식 중 하나다.

- `auto`: 복습 시기가 된 오답, 최근 평가 오답, 최근 학습 오답, 최근 학습 순으로 실제 단원 목록에 존재하는 범위를 추천한다. 기록이 없으면 검증된 첫 단원을 사용한다.
- `assigned`: 관리자가 실제 단원 목록에서 지정한다. 해당 과목은 같은 단원의 기존 퀴즈를 완전히 제출한 기록이 확인될 때까지 다른 단원으로 이동하지 않도록 앱 화면에서 안내하고 이동을 막는다. 계획 시간은 학습 안내이며 완료 조건이 아니다.

설정 변경은 아직 시작하지 않은 오늘 계획에는 반영된다. 오늘 과제 하나라도 시작하거나 완료했다면 현재 계획의 단원, 상태, 시작 시각, 임시 세션과 시간은 그대로 두고 새 설정은 다음 계획부터 적용한다. 과거 계획은 변경하지 않는다. 기존 `publisher` 서버 필드는 호환을 위해 보존하지만 화면과 새 저장 데이터에서는 사용하지 않는다.

설정 읽기에 실패하면 마지막으로 확인한 로컬 캐시를 유지한다. 캐시도 없으면 45분·초6·2학기·자동 추천을 표시한다. 과목 화면을 직접 연 상태에서 필수 범위를 온라인이나 캐시로 확인할 수 없으면 먼저 오늘 학습을 열라는 제한 안내를 표시한다.

Firestore 규칙은 설정 쓰기를 실제 Firebase `access/{uid}.role == admin` 계정으로 제한하고 스키마와 단원 식별자 형식을 검사한다. 과목 화면의 이동 제한은 사용자 흐름을 위한 UI 정책이며 보안 통제가 아니다. 브라우저 저장소 조작, 코드 직접 실행, 같은 Firebase 관리자 UID를 공유한 기기까지 막지는 못한다. 로컬 프로필 이름을 `우준아빠`로 바꾸는 것만으로 서버 관리자 권한을 얻을 수는 없지만, 관리자 UID로 로그인한 채 로컬 학습자 프로필을 쓰면 서버에서는 여전히 관리자 계정이다.
