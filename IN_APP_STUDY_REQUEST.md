# 무료 앱 안 공부 요청

이 기능은 Firebase Spark 무료 요금제에서 Firestore 직접 문서만 사용한다. 관리자 화면의 `공부 요청`은 학습자별 `learnerStudyRequests/{learnerId}` 최신 1문서를 `pending`으로 저장한다. 학습자가 앱을 열어 둔 경우 배너가 바로 갱신되고, 닫혀 있으면 다음 실행 때 표시된다. 운영체제 푸시, 백그라운드 알림, Cloud Functions와 외부 서비스는 사용하지 않는다.

학습 페이지를 열기만 해서는 시작으로 기록하지 않는다. 학습 타이머가 첫 실제 활동 시간을 센 순간 또는 주간 학습 확인에서 첫 답안을 선택한 순간에만 `learnerActivity/{learnerId}` 최신 1문서를 transaction으로 기록한다. 그 활동의 실제 시각이 현재 요청 시각 이후일 때만 같은 transaction에서 요청을 `started`로 바꾼다. 네트워크 실패 시 학습자별 최신 활동 1개를 로컬에 보관하고 온라인·포커스 복귀 때 같은 `activityId`로 재시도한다.

같은 활동 ID의 서버 재전송은 한 번만 반영한다. 성공한 활동 뒤 15분 안의 페이지 이동은 새 활동으로 반복 기록하지 않으며, 그 사이 새 `requestId`가 도착한 경우에는 새 요청 확인을 위해 한 번 기록할 수 있다. 관리자는 서버 요청·확인 시각과 학습자 기기 기준 실제 활동 시각을 구분해 본다. 기기 시계의 작은 오차는 5분까지 허용한다.

Firestore 규칙은 새 요청을 관리자에게만 허용하고 60초 재요청 제한을 둔다. 학습 시작 갱신은 해당 학습자를 사용할 수 있는 인증 계정만 수행하며 요청 ID·요청자·요청 시각은 바꿀 수 없다. 이 기능은 앱 안 상태 표시이므로 브라우저나 앱이 닫힌 상태에서 알림을 보내지 않는다. 화면의 `푸시 알림 별도 설정`은 기존 별도 기능이며 이 무료 공부 요청에 필요하지 않다.
