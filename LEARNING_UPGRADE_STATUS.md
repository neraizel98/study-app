# 학습 업그레이드 구현 상태

## 완료된 기능

- 수학 공식 퀴즈
  - 첫 제출의 문항, 선택 답안, 정답, 힌트 사용, 활동 시간을 하나의 보고서에 저장합니다.
  - 재시도는 같은 세션 보고서를 갱신하고 최초 답안은 유지합니다.
  - 오답 원문과 선택지를 보존하여 공식 화면에서 다시 풀 수 있습니다.
  - 공식 학습 시간은 `formulaStudyTime`에만 합산하여 일반 과목 시간과 중복되지 않습니다.
- 오늘 학습
  - 학습자별 학년, 학기, 선택 출판사 설정과 30/45/60분 계획을 지원합니다.
  - 시간 배분은 각각 국어/영어/수학 8/10/12, 12/15/18, 15/20/25분입니다.
  - 복습 도래 오답, 최근 평가 오답, 최근 취약 단원, 최근 학습 순으로 추천합니다.
  - 초6 프로필에는 중등 수학을 자동 배정하지 않으며 현재 학기 수학만 추천합니다.
  - 기록이 없으면 사용자가 단원을 선택합니다. 내용 학습 링크와 문항 연습을 구분합니다.
  - 제출 기록과 목표 시간은 별도로 표시하며, 제출 뒤에도 내용 학습과 새 문항 연습을 계속할 수 있습니다.
  - 실제 학습·퀴즈 시간과 오늘의 수학 공식 시간은 한 번씩만 합산합니다.
- 계정별 이어 풀기
  - 출제 문항, 답안, 현재 위치, 과목별 실제 활동 시간을 학습자와 세션별로 보관합니다.
  - 새로고침 후 같은 기기와 계정에서 이어 풀 수 있습니다.
  - 제출 중 답안을 고정하며 부분 저장 후 재시도해도 이미 저장한 과목 보고서를 중복 생성하지 않습니다.
  - 다른 탭에서 계정이 바뀌면 저장과 제출을 중단합니다.
- 주간 학습 확인
  - 이번 주 실제 학습 보고서가 있는 범위만 평가에 사용합니다.
  - 수학은 해당 단원의 검증된 `medium` 생성 문항, 국어는 해당 단원의 검증된 표준 지문 문항을 사용합니다.
  - 영어는 실제 학습 어휘의 기존 예문과 뜻 단서를 사용한 빈칸 재확인 문항입니다.
  - 과목별 요청 수와 실제 출제 수를 표시하고, 부족해도 미학습 단원을 채워 넣지 않습니다.
  - 제출 전에는 정답과 해설을 표시하지 않습니다. 제출 후 원문, 선택 답안, 정답, 해설을 보고서에 보존합니다.
  - 평가 보고서는 적응 난이도 승급 계산에서 제외합니다.
- 부모 화면
  - 최근 평가의 과목별 점수, 요청/실제 문항 수, 보존 답안 수를 표시합니다.
  - 최근 평가 범위·표본과 다음 보충 단원을 별도로 표시합니다.

## 저장과 동기화

- 퀴즈와 평가 결과는 기존 분할 보고서 저장 및 Firebase 동기화 경로를 사용합니다.
- 학습 계획과 미제출 초안은 계정별 기기 저장소에 보관하며 데이터 내보내기 대상인 `SmartStudy_*` 키를 사용합니다.
- 기존 보고서, 최초 답안, 오답 이력과 IndexedDB 원본 보관 구조를 변경하거나 삭제하지 않습니다.

## 검증 결과

- `node --test *.test.js`: 21/21 통과
- `SMARTSTUDY_BACKUP_PATH=... node partition-storage.test.js`: 실제 개인 백업의 보고서, 최초 답안, 오답 이력 보존 통과
- `learning-upgrade.test.js`: 시간 배분, 프로필 분리, 추천 범위, 완료 증거, 초안 격리, 학습 범위 평가, 오답 재출제 필드 통과
- Edge 모바일 390px 브라우저 검사:
  - 오늘 계획 생성, 단원 선택, 문항 연습 진입
  - 답안 새로고침 복원
  - 계정 변경 제출 차단
  - 평가 제출과 보고서 저장
  - 오늘 연습 제출 후 과제 완료 판정
  - 가로 넘침 없음
- Edge 수학 공식 검사: 첫 제출, 재시도, 최초 답안 유지, 활동 시간, 원문 오답 재출제 통과
- Edge 관리자 검사: 1011개 기존 보고서 로드, 지연 상세 답안, 주간 통계, 최근 평가 범위와 다음 보충 표시 통과

## 알려진 제약

- 미제출 초안은 같은 기기에서만 이어집니다. 제출 결과는 기존 클라우드 동기화 대상입니다.
- 영어 평가 문항은 학습한 기존 예문을 빈칸으로 다시 확인합니다. 새로운 상황의 영어 실력 평가로 표현하지 않습니다.
- 국어 평가는 현재 제공되는 검증 지문 문항 범위 안에서만 출제합니다.
- 주간 평가 문항 수가 부족하면 실제 수를 표시합니다. 부족분을 미학습 단원으로 보충하지 않습니다.
- 브라우저 자체 저장을 사용하는 자기 학습 기능이며 보안 시험 환경을 제공하지 않습니다.
