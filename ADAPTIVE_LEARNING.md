# 적응형 학습 · 오답 복습 구현 기록

적용 버전: 20260911-adaptive (학습 기록 정책 20260911-v1)

## 난이도
- 과목/단원별 기초 → 표준 → 응용. 문법은 단계와 단원, 수학은 학년·학기·단원, 독해는 레벨·단원으로 분리한다.
- 최근 최대 5회에서 유효 문항 20개 이상, 서로 다른 학습 날짜 2일 이상일 때 판정한다.
- 최근 3회 각각 90% 이상이면 한 단계 높인다. 최근 2회 각각 70% 미만이면 한 단계 낮춘다. 변경 후 새 기록을 모은다.
- 재시험·힌트·도움받은 쓰기·복습 전용 시험·구버전 기록은 승급 근거에서 제외한다. 같은 문항은 7일 안에 다시 승급 근거로 세지 않는다.
- 영어는 기초 객관식에서 회상·문맥 적용 비중을 늘리고, 문법은 검증된 단어 입력 비중을 조절한다. 수학은 기존 기초/심화 생성기를 사용하며, 독해는 선택 단원 안에서 지문 난이도를 조절한다. 한자는 쓰기 비중을 조절한다.

## 오답 복습
- 오답 → 풀이 확인/재도전 → 정답으로 고친 시점부터 1일 후 확인 → 그로부터 2일 후 → 그로부터 4일 후 확인이다. 제시간에 복습하면 교정 후 1·3·7일 일정이다.
- 세 번의 지연 확인을 통과하면 기억 확인 완료로 표시하고 30일 후 재확인한다. 다시 틀리면 처음부터 시작한다.
- 같은 세션, 즉시 반복, 힌트를 사용한 정답으로 지연 확인 단계를 올리지 않는다.
- 일반 출제는 복습 시기가 된 문제를 20~30% 우선 배정한다. 새 문항이 부족하면 기존 문제를 추가할 수 있다. 오답 전용 모드는 일정보다 일찍 연습할 수 있지만 단계가 빨리 올라가지는 않는다.
- 최초 답안은 보존하고 최신 재시험 답안은 별도로 저장한다. 동기화는 이벤트를 합친 후 동일 규칙으로 상태를 계산한다.

## 함께 수정한 오류
- 직육면체의 세로를 구하면서 세로 길이를 알려 주던 문장을 수정했다.
- 한자 쓰기는 실제 획순 완료 전 완료 버튼이 잠긴다. 실수·힌트·리셋을 거친 쓰기는 독립 정답으로 인정하지 않는다.
- 영어 복습에서 다른 레벨 단어의 오답 기록이 누락될 수 있던 경로를 수정했다.
- 영어 정답 버튼 중복 처리 방지, 수학 오답 식별자의 단원 분리, 문법/독해 난이도 판정의 단원 분리를 적용했다.
- 부모 화면에 단원별 난이도, 유효 최초 풀이 수, 복습 도래 수, 기억 확인 완료 수를 추가했다.

## 검증과 범위
- 자동 검사 19개 파일 통과. 추가 검사: 승급/하향, 같은 날 반복, 힌트, 최초 오답 보존, 중복 저장, 기기 간 병합, 쓰기 완료 잠금, 독해 단원 지문 존재 여부, 직육면체 500회 생성.
- 기존 콘텐츠 검사도 통과했다. 문법 생성 29,600건, 영어 예문 653개, 독해 27지문/149문항, 한자 460자 데이터 범위를 검사한다. 자동 검사 통과가 모든 교육적 표현의 완전성을 보장하지는 않는다.
- 변형은 기존 검증된 문제은행과 수학 생성기 범위에서 수행한다. 새로운 자유 서술형 자동 채점이나 모든 개념의 새 상황 문항 생성은 포함하지 않았다.
- 실제 휴대전화 화면과 실제 계정의 온라인 동기화는 이번 실행에서 직접 검증하지 않았다. 실제 사용자 기록을 수정하거나 초기화하지 않았다.
- 구현과 자동 검증을 완료한 로컬 변경이다. 이번 변경은 아직 운영 사이트에 배포하지 않았다.
