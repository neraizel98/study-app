# Smart Study 앱 구현 현황 심층 보고서

- 작성일: 2026-08-06
- 분석 대상: `study-app-grammar-m23` 전체
- Git 브랜치: `agent/english-tts-reading-integrity`
- 기준 HEAD: `1f085d5`
- 추가 분석 범위: 커밋되지 않은 국어 독해력 확장 변경 포함

## 1. 한눈에 보는 결론

Smart Study는 GitHub Pages에서 실행되는 정적 웹/PWA 학습 앱이다. 별도 애플리케이션 서버나 빌드 과정 없이 HTML, CSS, JavaScript 데이터 파일을 브라우저가 직접 읽는다. `localStorage`를 즉시 사용 가능한 주 저장소로 사용하고, Firestore를 기기 간 동기화와 관리자 조회용 원격 저장소로 사용한다.

정규 과목은 다음 다섯 개다.

1. 국어 독해력
2. 영어 단어
3. 영어 문법
4. 한자
5. 수학

수학 공식 사전은 별도 보조 학습 영역이다. 일반 과목의 퀴즈 잠금과 미션에는 들어가지 않지만 실제 활동 시간은 별도 필드로 기록되어 관리자 화면에 합산 표시된다.

앱의 핵심은 콘텐츠 자체보다 다음 흐름의 연결에 있다.

```text
사용자 선택 → 레벨별 학습 → 활성 시간 누적 → 퀴즈 잠금 해제
→ 최근 성적 기반 난이도 조정 → 문항별 정오답 저장
→ 오답 반복 → 통계·미션·보호자 공유 → Firestore 동기화
```

현재 구조는 기능 범위가 넓고 데이터 검증도 상당히 갖춰졌다. 저장소에는 84개 파일, 60개 JavaScript 파일, 13개 HTML 페이지, 14개 테스트 파일이 있다. 다만 인증 없는 이름 기반 사용자 구분, 전역 객체 중심 구조, 일부 페이지의 대형 인라인 코드, 한 개의 오래된 테스트 계약은 앞으로 정리할 필요가 있다.

## 2. 실행 및 배포 구조

### 2.1 정적 페이지 구조

주요 진입점은 다음과 같다.

| 페이지 | 역할 |
|---|---|
| `index.html` | 사용자 선택, 홈, 과목 목록, 미션·보상 |
| `reading.html` | 국어 독해 학습 및 퀴즈 |
| `english.html` | 영어 단어 학습 및 퀴즈 |
| `english_grammar.html` | 영어 문법 학습 및 퀴즈 |
| `hanja.html` | 한자 학습, 획순, 퀴즈 |
| `math.html` | 수학 레벨·단원 선택 |
| `math_viewer.html` | 수학 개념 학습 |
| `math_quiz.html` | 수학 퀴즈 |
| `math_formula.html` | 수학 공식 1~60 학습 및 계산 퀴즈 |
| `stats.html` | 학습자용 학습 현황과 퀴즈 기록 |
| `wrong_note.html` | 과목별 오답과 오답 다시 풀기 |
| `report.html` | 학습 결과·공유 보고서 |
| `admin.html` | 관리자 사용자·시간·성적·오답·설정 조회 |

각 HTML이 필요한 스크립트를 순서대로 불러오며, 공통 기능은 `window` 전역 객체로 연결된다. 번들러나 TypeScript가 없으므로 스크립트 로드 순서 자체가 의존성 계약이다.

### 2.2 GitHub Pages 배포

`main` 브랜치에 push되면 GitHub Actions가 다음 순서로 배포한다.

1. 저장소 checkout
2. Node.js 22 설치
3. 모든 `*.test.js` 실행
4. 테스트가 전부 통과한 경우에만 Pages 아티팩트 생성
5. GitHub Pages 배포

초기 보고서와 달리 현재 워크플로에는 테스트 단계가 존재한다. 따라서 현재 로컬에서 실패하는 테스트가 해결되지 않으면 `main` 배포도 중단된다.

## 3. 공통 애플리케이션 계층

### 3.1 과목 레지스트리

`report.js`의 `SubjectRegistry`가 다섯 정규 과목의 ID, 이름, 아이콘, 경로를 관리한다. 홈, 미션, 통계와 공유 기능은 가능한 범위에서 이 레지스트리를 사용한다. 다만 관리자와 일부 차트에는 아직 과목 배열과 색상이 별도로 하드코딩되어 있다.

### 3.2 저장소 계층

구조 개선 이후 직접 저장소 접근은 다음 계층으로 모였다.

- `storage-keys.js`: 모든 localStorage 키 정의
- `local-repository.js`: localStorage 읽기·쓰기와 마이그레이션 적용
- `firestore-repository.js`: Firestore 접근 단일 지점
- `firebase-sync.js`: 로컬과 원격 데이터 병합 및 지연 업로드
- `storage-events.js`: 저장 이벤트 통지
- `schema-migrations.js`: 스키마 버전별 순차 변환

`architecture-boundaries.test.js`는 다른 파일의 직접 localStorage·Firestore 접근, 런타임 저장 함수 덮어쓰기, 중복 외부 SDK 로드와 수학 공식 API 중복 등록을 막는다.

### 3.3 현재 스키마 버전

| 데이터 | 버전 | 주요 내용 |
|---|---:|---|
| 사용자 | 3 | 월간 미션, 공식 사전 시간 포함 |
| 퀴즈 보고 | 2 | `updatedAt` 포함 |
| 오답 | 2 | 과목별 `subjects` 구조 |
| 학습시간 설정 | 2 | 레벨별 설정 포함 |
| 적응형 점수 | 1 | 과목·맥락별 점수 기록 |

미래 버전 데이터를 현재 코드가 억지로 읽지 않도록 차단하며, 각 단계는 반드시 버전을 1씩 올려야 한다.

## 4. 사용자와 동기화

### 4.1 사용자 식별

현재 로그인은 인증 계정이 아니라 사용자가 입력한 이름을 기준으로 한다. 활성 사용자는 `SmartStudy_ActiveUser`에 저장되고, 사용자별 데이터·보고·오답은 별도 키로 분리된다.

이 방식은 가족 내부 사용에는 간단하지만 보안 인증은 아니다. Firebase Authentication이 없으므로 Firestore Security Rules가 충분히 엄격하지 않다면 다른 사람이 사용자 이름을 추측해 데이터에 접근할 위험이 있다.

### 4.2 주요 사용자 데이터

- 레벨, 경험치, 배지
- 누적 학습 시간·퀴즈 횟수·정답 수
- 출석과 연속 학습일
- 일간·주간·월간 통계
- 과목별 학습·퀴즈 시간과 점수
- 미션 진행 및 지급 보상
- 수학 공식 사전 학습·퀴즈 시간

### 4.3 동기화 흐름

페이지는 로컬 데이터를 먼저 표시하고 Firebase가 준비되면 원격 데이터를 내려받아 병합한다. 이후 변경은 약 2초 지연 후 업로드된다. 네트워크가 불안정해도 로컬 학습을 계속할 수 있지만, 서로 다른 기기에서 동시에 변경하면 큰 값 우선 병합 때문에 일부 독립 증가분이나 오답 history가 완벽히 합쳐지지 않을 수 있다.

## 5. 학습시간과 퀴즈 잠금

### 5.1 적용 단위

학습시간은 과목별 레벨 맥락으로 관리한다.

- 국어 독해력: Lv.1
- 영어 단어: Lv.1~Lv.4
- 영어 문법: Lv.1 초등, Lv.2 중1, Lv.3 중2, Lv.4 중3
- 한자: 8급~5급
- 수학: 초6, 중1 및 학기·단원 맥락

관리자는 `admin.html`에서 과목·레벨별 최소 시간을 설정한다. 값이 없으면 과목 기본값, 그마저 없으면 5분을 사용한다.

### 5.2 최근 세 번 점수 규칙

같은 과목·맥락의 최근 3회 백분율 평균을 사용한다.

| 평균 | 점수 기준 학습시간 |
|---|---:|
| 90점 이상 | 면제 |
| 80점 이상 | 5분 |
| 70점 이상 | 10분 |
| 70점 미만 | 15분 |
| 기록 없음 | 관리자 설정값 |

90점 미만일 때는 관리자 설정시간과 점수 기준 시간 중 더 긴 시간을 적용한다. 퀴즈가 완료되면 새 점수를 기록하고 해당 학습 맥락의 잠금용 누적 시간을 초기화한다. 수학 퀴즈 URL 직접 접근도 동일한 조건을 검사한다.

### 5.3 자리비움 제외

공통 `ActiveTimeTracker`와 `StudyTimer`는 다음 조건을 이용한다.

- 페이지가 보이는 상태
- 필요한 경우 브라우저 포커스 유지
- 현재 학습 또는 퀴즈 모드
- 최근 클릭·키보드·터치·스크롤 활동 후 45초 이내
- 한 번의 측정에서 비정상적으로 큰 시간 증가 제한

현재 일반 과목 보고에 전달되는 `timeSpentSeconds`는 실제 활동 퀴즈 시간으로 정리되어 있으며, 학습 시간과 퀴즈 시간은 일간 통계에서 구분한다. 관리자와 공유 보고서는 두 시간을 합산해 과목별 실제 활동 시간으로 표시한다.

## 6. 퀴즈·오답·적응형 난이도

### 6.1 퀴즈 보고

`saveQuizResult`는 `sessionId`를 기본 식별자로 사용하며 다음 값을 저장한다.

- 과목과 레벨·단원
- 전체 문항 수
- 최초 점수와 최종 점수
- 활성 퀴즈 시간
- 완료 여부
- 과목별 metadata
- 실제 출제 문항 스냅샷과 선택 답안

같은 세션의 오답 재도전은 새 보고를 계속 추가하지 않고 기존 보고의 최종 점수와 시간을 갱신한다. 시간은 이전 값과의 차이만 통계에 더해 중복 합산을 막는다.

### 6.2 오답 노트

오답은 과목별로 보관하며 문제, 선택 답, 정답, 설명, 세션, 회차와 기록 시각을 저장한다. 미숙련 오답은 전용 review URL로 전달되어 일반 학습 잠금 없이 해당 문제만 다시 풀 수 있다. 연속 정답으로 숙련 여부를 갱신한다.

영어 문법과 국어 독해력은 실제 출제 당시의 문항·선택지·지문 스냅샷을 저장한다. 따라서 랜덤 생성 후 다른 문제로 바뀌어 오답 내역과 시험 화면이 불일치하는 문제를 방지한다.

### 6.3 적응형 출제

| 구간 | 최근 성적 | 오답 우선 비율 | 출제 방향 |
|---|---:|---:|---|
| foundation | 70점 미만 | 55% | 기초 반복 |
| standard | 70~84점 | 45% | 기본·오답 균형 |
| challenge | 85점 이상 | 35% | 새 문제와 응용 확대 |

오답 횟수, 최근 오답 여부, 숙련 상태가 우선순위에 반영된다. 국어 독해력은 심화 단계에서 15줄 이상 지문을 80% 확률로 우선 선택하도록 추가 조정되어 있다.

## 7. 과목별 구현

### 7.1 국어 독해력

현재 Lv.1이며 중1 독해 요소를 초6 학생도 이해할 수 있는 설명으로 제공한다.

- 중심 문장과 제목
- 두괄식·미괄식 및 문단 관계
- 비교·대조·과정·인과 전개
- 사실 확인과 추론
- 주장과 근거
- 이야기 구조와 인물 갈등
- 시적 표현
- 고전·현대 문학 읽기

현재 로컬 변경 기준으로 27개 지문, 총 357개 검증 문항 풀을 가진다. 지문마다 본문 문제 3~5개에 문맥 어휘와 사자성어 문제가 결합된다.

콘텐츠 범위에는 기본 상식, 과학·환경, 미디어 문해, 고전·현대 문학, 시, 고전 철학, 교육, 한국사와 세계사가 포함된다. 철학은 소크라테스·플라톤·아리스토텔레스·공자의 사상을 일상 사례로 풀어 썼으며, 15~20줄과 20~30줄의 고난도 지문도 존재한다.

역사 지문에는 추후 별도 과목으로 분리할 수 있도록 다음 메타데이터를 저장한다.

- 한국사/세계사 미래 과목 ID
- 지역, 시대 구분, 세기와 핵심 연도
- 학교급·학년·교과 영역
- 주제, 사건, 인물
- 역사 역량과 자료 유형

`ReadingHistoryCatalog.filter()`는 과목, 시대, 학교급, 학년, 주제, 역사 역량 조건으로 지문을 검색할 수 있다. 현재는 국어 독해 풀에 포함되지만 추후 역사 과목이 같은 원본 데이터를 재사용할 수 있다.

### 7.2 영어 단어

- Lv.1~Lv.4
- 단어 뜻, 문장, 동사 변화, 구동사
- 객관식, 문맥, 형태 변화, 문장 배열, 직접 입력
- 오답 우선 랜덤 출제와 같은 세션 재도전
- 미국식·영국식 음성 버튼

`english-speech.js`는 브라우저가 제공하는 음성 중 요청 언어와 국가·음성 이름을 기준으로 미국·영국 음성을 고른다. 모바일과 PC의 설치 음성 품질 차이는 여전히 운영체제 음성 목록에 영향을 받는다. 외부 유료 TTS 음원을 사용하지 않으므로 모든 PC에서 동일한 원어민 음질을 보장할 수는 없다.

### 7.3 영어 문법

- Lv.1 초등, Lv.2 중1, Lv.3 중2, Lv.4 중3
- 학년별 대단원과 소단원
- 객관식, 배열, 오류 수정, 직접 입력
- 자연스러운 영어 예문 검증 규칙
- 미국식·영국식 읽기
- 레벨 단위 학습시간 잠금
- 문법 영역별 정오답 기록과 취약점 분석
- 소단원 다음 이동 시 같은 레벨 안에서 다음 대단원으로 연결

### 7.4 한자

- 8급~5급, 총 460개 항목의 획순 데이터 직접 또는 별칭 연결 확인
- 음·뜻·부수·숙어·유의어·반의어
- 뜻, 역방향, 부수, 숙어, 유의·반의 및 쓰기 문제
- HanziWriter 기반 획순 표시

쓰기 문제의 채점은 필기 모양을 AI로 판독하는 방식이 아니라 사용자의 완료 확인에 의존한다.

### 7.5 수학

- 초6 과정
- 중1 1학기와 2학기
- 학습, 단원 선택, 퀴즈 페이지 분리
- 기본 계산·심화 연산·서술형을 포함한 랜덤 생성
- 오답과 최근 성적에 따른 난이도 조정
- 직접 URL 접근 잠금 검사

### 7.6 수학 공식 사전

- 공식 1~60, 10개 그룹
- 초6~고3 수준 필터
- 공식, 기호, 선수 지식, 원리, 예제, 도형
- 모바일 메뉴 접기와 본문 자동 이동
- 매회 계산·원리 이해 객관식 3문항
- 일반 미션과 잠금 제외
- 학습·퀴즈 활성 시간은 별도 집계 후 관리자에 표시

## 8. 통계·보고·관리자

### 8.1 학습자 통계

`stats.html`은 주간 단위로 다음을 제공한다.

- 요일별·과목별 학습시간
- 퀴즈 횟수와 평균 점수
- 최근 8주 과목별 점수 추이
- 과목·레벨·단원별 성취도
- 실제 출제 문항의 정답·오답 상세

점수 그래프는 50~100 범위를 사용하고 50점 미만은 점을 50 위치에 두되 실제 점수 라벨은 유지한다.

### 8.2 공유 보고서

카카오 공유는 학습 요청, 단일 퀴즈 결과, 일일·최근 요약을 지원한다. 공유 데이터는 별도 서버 문서가 아니라 Base64로 인코딩되어 `report.html?import=...` URL에 포함된다. 구현은 간단하지만 URL을 가진 사람이 내용을 볼 수 있고 브라우저·메신저 기록에 남을 수 있다.

### 8.3 관리자

관리자 화면은 다음을 표시한다.

- 사용자 레벨, 경험치, 출석, 누적 학습
- 오늘 과목별 학습+퀴즈 활성 시간
- 최근 퀴즈와 실제 문항 상세
- 주간·과목별·레벨별 통계
- 영어 문법·국어 독해 유형 분석
- 과목별 오답 현황
- 미션 보상 사용 처리
- 과목·레벨별 최소 학습시간 설정
- 수학 공식 사전 학습·퀴즈 시간

## 9. 미션과 보상

미션은 다음 시점 이후 앱이 다시 상태를 점검할 때 새 기간으로 초기화된다.

- 일간: 매일 0시
- 주간: 매주 월요일 0시
- 월간: 매월 1일 0시

일간·주간·월간은 출석, 학습시간, 학습 과목 수, 점수 또는 퀴즈 횟수를 조합한다. 정규 다섯 과목은 `SubjectRegistry`를 통해 모든 과목 완료 판정에 포함된다. 각 기간의 모든 미션을 완료하면 한 번만 보상을 무작위 지급한다.

## 10. PWA와 캐시

`manifest.json`과 아이콘을 통해 홈 화면 설치를 지원한다. 서비스 워커는 현재 `smart-study-v56-active-subject-time` 캐시를 사용한다.

- HTML·JavaScript: 네트워크 우선, 실패 시 캐시
- 이미지 등 정적 자원: 캐시 우선
- 새 버전 활성화 시 이전 캐시 제거
- 로컬 선캐시 파일 존재 여부를 아키텍처 테스트로 검증

`version-check.js`는 배포 버전과 앱 버전을 비교하여 업데이트 UI를 표시한다.

## 11. 검증 결과

2026-08-06 현재 14개 테스트를 직접 실행했다.

통과한 13개 테스트:

- 활성 학습·퀴즈 시간 및 자리비움 제외
- 저장소·Firestore·API 경계
- 관리자 및 통계 UI
- 영어 음성·문법 문항·국어 스냅샷 무결성
- 영어 문법 활성 시간과 교육과정
- 한자 460자 획순 데이터 범위
- 수학 공식 시간 분리
- 미션 기간과 보상
- 국어 27지문·357문항 무결성
- 스키마 마이그레이션
- 레벨별 학습 잠금
- 다섯 과목 통합

실패한 1개 테스트:

- `math-formula.test.js`

실패 원인은 기능 파일 누락이 아니라 테스트가 `report.js?v=20260730-architecture`라는 과거 캐시 버전 문자열을 정확히 요구하는 반면, 현재 HTML은 `report.js?v=20260804-active-time`을 로드하기 때문이다. `MathFormulaTime.js`는 실제로 `UserSession`을 사용하고 `firebase-sync.js`도 로드되므로 현재 증거상 테스트 계약이 오래된 것으로 판단된다. 그러나 GitHub Actions는 이 실패로 배포를 막으므로 업로드 전에 테스트를 버전 문자열이 아닌 스크립트 존재 기준으로 수정해야 한다.

## 12. 남은 위험과 개선 우선순위

### 우선순위 높음

1. `math-formula.test.js`의 오래된 캐시 버전 문자열 의존 제거
2. Firebase Authentication과 보호자·학습자 권한 모델 도입
3. Firestore Security Rules와 Emulator 테스트를 저장소에 포함
4. 공유 URL에 들어가는 학습 정보의 노출 범위 축소 또는 만료 가능한 공유 방식 도입

### 데이터 신뢰성

1. 여러 기기에서 동시에 증가한 통계를 이벤트 ID 기반으로 병합
2. 오답 history를 최신 항목 승리 방식이 아닌 이벤트 합집합으로 병합
3. 사용자·보고·오답의 `createdAt`, `updatedAt`, 생성 기기 ID를 일관되게 관리
4. 실제 브라우저에서 학습→잠금→퀴즈→통계까지 종단간 테스트 추가

### 유지보수성

1. `admin.html`, `stats.html`, `report.html`의 인라인 로직을 모듈로 분리
2. 관리자에 남은 과목 이름·색상 하드코딩을 `SubjectRegistry`로 통합
3. 전역 `window` 객체 의존을 ES module 또는 명시적인 앱 컨테이너로 축소
4. 콘텐츠 스키마를 공통 검증기로 통합

## 13. 신규 과목 추가 기준

새 과목은 콘텐츠 페이지만 추가해서는 완전하게 동작하지 않는다. 다음을 모두 확인해야 한다.

1. `SubjectRegistry` 등록과 홈 배치
2. 학습 데이터와 퀴즈 생성기
3. 레벨별 `StudyTimer` 설정과 URL 직접 접근 차단
4. 활성 학습·퀴즈 시간 기록
5. `saveQuizResult` 문항 스냅샷
6. 오답 식별자와 review URL
7. 적응형 난이도 및 오답 우선 출제
8. 학습자 통계, 보고서, 카카오 공유
9. 관리자 시간·점수·취약 영역·오답 분석
10. 일간·주간·월간 미션의 모든 과목 판정
11. Firestore 병합과 스키마 마이그레이션
12. 서비스 워커 및 버전 갱신
13. 정답 정확성, 선택지 고유성, 랜덤성, 시간, 통합 테스트

역사 과목을 추가할 때는 이미 국어 지문에 마련된 `ReadingHistoryCatalog`를 원천 데이터로 활용하되, 별도 과목에서는 시대 순서·교과 성취기준·사료 유형·역사 역량별 학습 화면을 추가하는 방식이 적합하다.

## 14. 2026-08-06 개선 적용 및 재검증

이 문서에서 확인했던 실패 항목과 높은 우선순위 위험을 실제 코드에 반영했다. 현재 작업 트리의 재귀 파일 수는 103개이며 JavaScript 61개, HTML 13개, 테스트 15개다.

- 수학 공식 테스트는 캐시 무효화용 쿼리 문자열의 특정 날짜를 비교하지 않고 필요한 스크립트가 실제로 연결되었는지 검사한다.
- 보고 스키마는 v3으로 올리고 `createdAt`, `updatedAt`, `deviceId`를 표준 필드로 추가했다.
- 오답 스키마도 v3으로 올리고 각 이력에 안정적인 `eventId`, 생성 시각, 기기 ID를 기록한다.
- 여러 기기의 보고는 `updatedAt` 기준으로 병합하며, 오답은 최신 항목 하나만 남기지 않고 이벤트 합집합을 만든 뒤 최근 15개를 유지한다.
- 카카오 공유 데이터는 버전·생성 시각·7일 만료 시각을 가진 봉투 형식으로 바뀌었다. 전체 기록 공유는 최소 필드만 포함하고, 가져오기 전에 사용자의 확인을 받으며, 공유 화면 문자열을 HTML 이스케이프한다.
- Firebase Authentication의 Google 로그인을 추가했다. 인증되지 않은 경우 Firestore 동기화는 시작하지 않고 로컬 모드로 계속한다.
- `firestore.rules`는 `access/{uid}`의 역할과 허용 학습자 목록을 기준으로 접근을 제한하며 그 밖의 문서는 기본 거부한다. 실제 보호는 `FIREBASE_SECURITY_SETUP.md`에 따라 Google 제공자, 접근 문서, 규칙을 Firebase에 배포한 뒤 활성화된다.
- 관리자와 학습 통계의 과목 목록·이름·아이콘은 `SubjectRegistry`에서 생성한다. 신규 과목은 차트와 관리자 집계에 자동 포함되고, 별도 색상이 없으면 안전한 기본색을 사용한다.

전체 15개 `*.test.js`를 다시 실행했으며 모두 통과했다. 새 `security-and-sync.test.js`는 스키마 v3, 공유 만료·최소화, 인증 SDK·기본 거부 규칙, 레지스트리 기반 과목 집계를 검증한다.

남은 운영 작업은 코드 결함이 아니라 Firebase 환경 설정이다. 규칙을 배포하기 전에 허용할 보호자·학습자 Google 계정의 UID와 `learnerIds`를 등록해야 한다. 브라우저 음성 품질은 운영체제에 설치된 음성 목록에 의존한다는 제약도 그대로다. 장기적으로는 대형 인라인 페이지 로직 분리와 ES module 전환이 가능하지만, 현재 기능 정확도나 배포를 막는 항목은 아니다.

## 15. 최종 평가

현재 앱은 단순 문제집이 아니라 학습시간 통제, 최근 성적 기반 조정, 오답 반복, 장기 통계, 미션 보상, 보호자 공유와 원격 관리가 연결된 개인 학습 플랫폼이다. 최근 구조 개선으로 저장소 접근, 스키마 마이그레이션, API 중복 방지와 활성 시간 집계가 이전보다 명확해졌다.

다음 단계의 핵심은 콘텐츠 양을 늘리는 것보다 다음 세 가지를 보장하는 것이다.

1. 어느 화면에서 보더라도 점수와 시간이 같은 뜻을 가질 것
2. 랜덤 생성 문제의 실제 출제 내용과 저장·오답·공유 내용이 항상 같을 것
3. 새 과목이 한 곳의 등록만으로 통계·미션·관리자까지 확장될 것

이 세 원칙을 유지하면 향후 한국사·세계사 등 신규 과목도 현재 기능을 빠뜨리지 않고 안정적으로 추가할 수 있다.
